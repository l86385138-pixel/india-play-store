# Enquiry admin fix

The admin Enquiries section is intended to support APK enquiry search, per-enquiry delete, and delete-all. The implementation is in `index.html`.

The live page may require GitHub Pages deployment/cache refresh before the changes become visible.